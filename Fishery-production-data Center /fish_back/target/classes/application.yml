server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/FisheryData
    username: 'root'
    password: 'keji283650'

mybatis-plus:
  configuration:
    # 关闭驼峰映射(非常重要，否则数据查询结果可能为null)
    map-underscore-to-camel-case: false